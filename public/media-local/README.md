Временное хранилище реальных кадров/постеров, пока не выбран и не настроен CDN
(Cloudflare R2 / Bunny — решение из `Задачи разработки/01_concept.md`).

Эта папка НЕ попадает в git (см. .gitignore) — файлы здесь только у тебя локально.
Указывать путь в проекте: cover: "/media-local/имя-файла.jpg"

Когда появится CDN — файлы переезжают туда, в frontmatter проектов меняются только пути.
