# 02 — Состояние проекта

Обновлено: 19 августа 2026.

## Что сделано

Каркас сайта на Astro, главная страница перенесена из прототипа один в один, двуязычие заложено в структуру.

**Работает:**

- Главная на двух языках: `/` (RU) и `/en` (EN). Сетка, фильтры, скраб с таймкодом, бургер-меню, кнопка «наверх» — всё из прототипа, поведение не менялось.
- Переключатель RU/EN в меню — ведёт на ту же страницу на другом языке.
- Роутинг главная → проект: клик по плитке открывает `/project/<id>` (и `/en/project/<id>`), внизу навигация «пред / след».
- 22 реальные работы перенесены в контент-файлы (коммит `0cafede`), все со статусом `draft: true`.
- Сборка: 8 страниц, ошибок нет. Страниц мало и сетка пустая — потому что все работы черновики.

**Заглушки, ждут решений:**

- Страница проекта — только герой, название, клиент/год/категории и честная надпись «не спроектирована». Ждёт спеки.
- «Обо мне», «Контакты», «Посты» — пустые страницы с заголовком. Ждут контента.
- Все 22 работы — `draft: true`: в сетку не попадают. Ждут кадров, ответов по вопросам из `03_список_работ.md` и решения, что можно показывать публично.
- Кадры — 6 картинок-заглушек из прототипа, лежат в `public/placeholder/`.

## Стек и решения

| Решение | Выбор | Почему |
|---|---|---|
| Сборка | Astro 7, статическая генерация | Контент отдельно от кода; i18n из коробки; на выходе обычный HTML, фреймворка в браузере нет |
| Языки | RU по умолчанию (`/`), EN с префиксом (`/en`) | Заложено сразу — добавлять потом дорого |
| Тексты интерфейса | `src/i18n/ui.ts` | Все строки в одном файле, в компонентах строк нет |
| Проекты | `src/content/projects/*.md` | Один проект — один файл; схема проверяется на сборке |
| Домен | `maxsobolev.com` | Куплен 19 августа 2026 на reg.ru. Канонический адрес — `https://maxsobolev.com`, прописан в `astro.config.mjs` |
| Хостинг сайта | Yandex Object Storage + CDN | Решено 30 августа 2026, заменяет Cloudflare Pages (рассматривали и Amvera Cloud — отклонили из-за цены, у неё нет бесплатного тарифа, от ~170 ₽/мес). Причина смены с Cloudflare: подтверждённые проблемы с доступом из России без VPN (см. ниже), а основная аудитория сайта сейчас — российская. У Yandex Cloud при трафике этого сайта хостинг практически бесплатный (первые 100 ГБ исходящего трафика в месяц не тарифицируются, хранение — единицы рублей). Деплой не push-to-deploy из коробки — через GitHub Actions (`.github/workflows/deploy.yml`, уже в репозитории): сборка `npm run build` → заливка `dist/` в бакет по S3-протоколу при каждом пуше в main. Обратная сторона решения: доступность сайта извне России под вопросом (есть прецедент блокировки Яндекса как сервиса в Латвии/Финляндии/Норвегии на уровне государства) — принято сознательно, т.к. сейчас важнее РФ-аудитория |
| Видео | Yandex Cloud Video | Решено 30 августа 2026, заменяет Bunny Stream. Автор специально уточнил про транскодирование/адаптивное качество — есть, отдельный сервис (не Object Storage): свой транскодер, свой плеер, HLS. Цена по прикидке ниже, чем у Bunny Stream (транскодирование ~0.81 ₽/мин разово при заливке, хранение ~1.8 ₽/ГБ/мес, трафик ~0.78 ₽/ГБ — но подтверждённой бесплатной квоты на трафик, в отличие от Object Storage, не нашли) |
| Картинки и лёгкие превью-клипы | Yandex Object Storage | Решено 30 августа 2026, заменяет Bunny CDN. Тот же бакет/аккаунт, что и сайт — один вендор на всё (сайт + картинки + видео через Cloud Video) вместо Cloudflare+Bunny. В репозиторий не кладём: медиа туда не хранится в принципе (см. правило в `CLAUDE.md`) |

## Структура

```
Portfolio_site/
├── Задачи разработки/        документы решений
├── work/                     прототип из чата (портфолио-прототип.html)
├── public/placeholder/       кадры-заглушки, 6 штук
└── src/
    ├── content/projects/     ← ПРОЕКТЫ: один файл = один проект
    ├── i18n/ui.ts            ← ТЕКСТЫ интерфейса, RU и EN
    ├── styles/global.css     весь CSS, перенесён из прототипа
    ├── components/           Header, Filters, Tile, ToTop
    ├── layouts/              Base, Home, ProjectPage, StubPage
    ├── lib/projectPaths.ts   маршруты проектов + соседи
    └── pages/                маршруты: /, /en, /project/[id], /en/project/[id]
```

## Как добавить проект

Создать файл `src/content/projects/имя-проекта.md`:

```markdown
---
title: "Название"
client: "Клиент"
year: 2026
categories: [vfx, character]  # character | vfx | motion2d | neuro
production: "shoot"           # shoot | cg — необязательно
cover: "/placeholder/frame-01.jpg"
order: 20260315                # дата ГГГГММДД, больше — новее и выше в сетке
draft: false                  # true — не показывать
---

Текст ниже — Markdown, это описание проекта и, если нужно, `## Credits`.
```

Про `videos` (ролики с плеем на странице проекта) — см. `04_страница_проекта.md`.

### Порядок и как его менять

`order` — дата в формате `ГГГГММДД` (например `20250315`), больше значит новее и
выше/левее в сетке (изменено 20 августа 2026 — раньше было произвольное число с
шагом 10, потом 100; сортировка в коде тоже развёрнута под это, см. `Home.astro` и
`projectPaths.ts`). Поправить можно в любой момент, просто поменяв цифры — это не
создаёт проблем с зазорами, дата по сути просто большое число.

**Когда точная дата внутри года неизвестна** (25 августа 2026) — не ставим везде
`01.01`: это создаёт кучу проектов на одном дне и нечем разрулить порядок внутри
года без пересчёта соседей. Вместо этого месяц (день всегда `00`) выбирается
делением пополам от центра года: первый проект в году — `0600` (середина),
следующий — середина большего из оставшихся пустых промежутков (при равных
промежутках берётся более ранний). Для 1–2 проектов в году: `0600`, `0300`. Так
всегда есть куда вписать реальную дату позже без пересчёта остальных. Порядок
внутри года можно и нужно поправлять руками под смысл (например, поставить более
удачный ролик раньше в году, чтобы он был виднее) — это уже сделано вручную для
нескольких проектов (Детская Сберкарта, ОСАГО, Hbox, Добрый Лось).

Имя файла становится адресом страницы: `unity.md` → `/project/unity`.
Если обязательное поле пропущено — сборка остановится и скажет какое.

## Команды

```
npm run dev      локальный сервер, http://localhost:4321
npm run build    сборка в dist/
npm run preview  посмотреть собранное
```

## Хостинг сайта — история решения (30 августа 2026)

Изначально был Cloudflare Pages (см. таблицу выше — решение сменилось). Ход
рассуждения:

1. 25 августа: обнаружено, что `b-cdn.net` (Bunny CDN) не открывается без VPN
   у автора; внешняя проверка показала `200 OK`, то есть дело не в
   конфигурации зоны, а похоже на блокировку на уровне провайдера/страны.
2. Тест через check-host.net по прямой ссылке на картинку: 2 из 3 российских
   узлов — ок, 1 — таймаут. Не полная блокировка, но нестабильно.
3. Проверили сам домен `maxsobolev.com` — отвечает (ping/http ок), но это
   ничего не говорило о доступности самого Cloudflare Pages: при проверке
   заголовков ответа (`Server: cloudflare`) стало ясно, что сайт крутится
   именно на Cloudflare — то есть под тем же риском, что и Bunny CDN.
4. Разобрали альтернативы для сайта: Amvera, Yandex Cloud Object Storage +
   CDN, VK Cloud Object Storage, Selectel — все российские, деплой чуть
   сложнее (кроме Amvera — она ближе всего к опыту Cloudflare Pages: git push
   → автосборка → деплой).
5. Отдельно проверили риск в обратную сторону — доступность российской
   инфраструктуры НЕ из России. Подтверждённый прецедент: в 2023 году
   Латвия, Финляндия и Норвегия заблокировали Яндекс целиком на уровне
   государства. По Amvera/VK Cloud/Selectel конкретных данных нет ни за, ни
   против.
6. Сначала выбрали Amvera Cloud (проще всего — git push, как Cloudflare Pages).
   Проверили цену: у Amvera нет бесплатного тарифа, от ~170 ₽/мес за минимальные
   ресурсы (почасовая/поминутная тарификация).
7. Сравнили с Yandex Cloud Object Storage: первые 100 ГБ исходящего трафика
   в месяц бесплатны, хранение — единицы рублей за ГБ. При трафике этого
   сайта хостинг практически ничего не будет стоить большинство месяцев.
   Дороже по настройке (нужен GitHub Actions вместо готового git push), но
   разово.
8. Решение: Yandex Cloud Object Storage + CDN — сейчас важнее российская
   аудитория и цена, разовая сложность настройки не пугает. Риск потери
   части зарубежной аудитории — принят сознательно, не забыт.

**Bunny CDN (картинки/видео)** — решение по нему не менялось, остаётся Bunny.
Тест показал 2/3, не полную блокировку; при дальнейших проблемах кандидат на
замену — Cloudflare Images/Stream или тот же путь, что для сайта.

## Дальше по плану

1. **Спека страницы проекта** — что на ней, какие блоки, как устроен breakdown. Пока не спроектирована.
2. Реальные проекты вместо заглушек.
3. Медиа: спрайты для скраба, видео на внешнем хостинге, картинки на CDN.
4. Контент для «Обо мне», «Контакты», «Посты».
5. Хостинг и деплой (в процессе, 30 августа 2026): workflow `.github/workflows/deploy.yml`
   в репозитории готов — при пуше в main собирает `npm run build` и заливает `dist/`
   в бакет Yandex Object Storage по S3-протоколу. Автору нужно вручную (не может
   сделать ассистент — создание аккаунта):
   1. Завести аккаунт в Yandex Cloud, привязать оплату.
   2. Создать бакет, включить в нём режим «статический сайт» (index.html).
   3. Привязать домен `maxsobolev.com` + SSL через Yandex Cloud CDN перед бакетом.
   4. Создать сервисный аккаунт со статическим ключом доступа.
   5. Добавить в репозиторий на GitHub секреты: `YC_ACCESS_KEY_ID`,
      `YC_SECRET_ACCESS_KEY`, `YC_BUCKET_NAME` (Settings → Secrets and variables → Actions).
   6. Когда заработает — перенаправить www → голый домен, снять DNS/подключение
      от Cloudflare Pages.
   Bunny (Stream + CDN) для медиа — без изменений.
