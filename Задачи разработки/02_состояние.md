# 02 — Состояние проекта

Обновлено: 19 августа 2026.

## Что сделано

Каркас сайта на Astro, главная страница перенесена из прототипа один в один, двуязычие заложено в структуру.

**Работает:**

- Главная на двух языках: `/` (RU) и `/en` (EN). Сетка, фильтры, скраб с таймкодом, бургер-меню, кнопка «наверх» — всё из прототипа, поведение не менялось.
- Переключатель RU/EN в меню — ведёт на ту же страницу на другом языке.
- Роутинг главная → проект: клик по плитке открывает `/project/<id>` (и `/en/project/<id>`), внизу навигация «пред / след».
- 12 проектов-заглушек из прототипа перенесены в контент-файлы.
- Сборка: 32 страницы, ошибок нет, уязвимостей в зависимостях нет.

**Заглушки, ждут решений:**

- Страница проекта — только герой, название, клиент/год/категории и честная надпись «не спроектирована». Ждёт спеки.
- «Обо мне», «Контакты», «Посты» — пустые страницы с заголовком. Ждут контента.
- Проекты — вымышленные (Nike, HBO, ESPN…), кадры — 6 картинок из прототипа, лежат в `public/placeholder/`.

## Стек и решения

| Решение | Выбор | Почему |
|---|---|---|
| Сборка | Astro 7, статическая генерация | Контент отдельно от кода; i18n из коробки; на выходе обычный HTML, фреймворка в браузере нет |
| Языки | RU по умолчанию (`/`), EN с префиксом (`/en`) | Заложено сразу — добавлять потом дорого |
| Тексты интерфейса | `src/i18n/ui.ts` | Все строки в одном файле, в компонентах строк нет |
| Проекты | `src/content/projects/*.md` | Один проект — один файл; схема проверяется на сборке |
| Хостинг | не решён | Статику примет любой; решаем перед деплоем |

## Структура

```
Portfolio_site/
├── Задачи разработки/        документы решений
├── work/                     прототип из чата (портфолио-прототип.html)
├── public/placeholder/       кадры-заглушки, 6 штук
└── src/
    ├── content/projects/     ← ПРОЕКТЫ: один файл = один проект
    ├── i18n/ui.ts            ← ТЕКСТЫ интерфейса, RU и EN
    ├── styles/global.css     весь CSS, перенесён из прототипа
    ├── components/           Header, Filters, Tile, ToTop
    ├── layouts/              Base, Home, ProjectPage, StubPage
    ├── lib/projectPaths.ts   маршруты проектов + соседи
    └── pages/                маршруты: /, /en, /project/[id], /en/project/[id]
```

## Как добавить проект

Создать файл `src/content/projects/имя-проекта.md`:

```markdown
---
title: "Название"
client: "Клиент"
year: 2026
categories: [vfx, cg]      # cg | vfx | realtime | direction
cover: "/placeholder/frame-01.jpg"
order: 0                   # меньше — выше в сетке
draft: false               # true — не показывать
---
```

Имя файла становится адресом страницы: `unity.md` → `/project/unity`.
Если обязательное поле пропущено — сборка остановится и скажет какое.

## Команды

```
npm run dev      локальный сервер, http://localhost:4321
npm run build    сборка в dist/
npm run preview  посмотреть собранное
```

## Дальше по плану

1. **Спека страницы проекта** — что на ней, какие блоки, как устроен breakdown. Пока не спроектирована.
2. Реальные проекты вместо заглушек.
3. Медиа: спрайты для скраба, видео на внешнем хостинге, картинки на CDN.
4. Контент для «Обо мне», «Контакты», «Посты».
5. Хостинг и деплой.
